# Uncomment the following lines to enable the Corne RGB Underglow
# CONFIG_ZMK_RGB_UNDERGLOW=y
# CONFIG_WS2812_STRIP=y

# Uncomment the following line to enable the Corne OLED Display
CONFIG_ZMK_DISPLAY=y

CONFIG_LV_USE_THEME_MONO=n

# Use a dedicated thread for UI updates
# Using a dedicated thread requires more memory but prevents
# displays with slow updates (e.g. E-paper) from delaying key
# scanning and other processes.
CONFIG_ZMK_DISPLAY_WORK_QUEUE_DEDICATED=y

# Increasing the transmit power of the keyboard's BLE radio
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Low level setting for GATT subscriptions. Set to n to work around
# an annoying Windows bug with battery notifications.
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n
